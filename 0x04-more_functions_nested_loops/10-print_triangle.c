#include "main.h"

/**
 * print_triangle - prints a triangle
 * followed by a new line
 * @size: integer
 */
void print_triangle(int size)
{
	if (size > 0)
	{
		int n
	}
	else
	{
		_putchar('\n');
	}
}

